<h2>🧠 Discover by Interests</h2>

<% if (matchedEvents.length > 0) { %>
<h3>🎟️ Events</h3>
<ul>
  <% matchedEvents.forEach(event => { %>
  <li>
    <a href="/events/<%= event._id %>"><strong><%= event.title %></strong></a>
    — Match Score: <%= event.score %>
  </li>
  <% }) %>
</ul>
<% } else { %>
<p>No matching events found.</p>
<% } %>

<% if (matchedCommunities.length > 0) { %>
<h3>🏘️ Communities</h3>
<ul>
  <% matchedCommunities.forEach(c => { %>
  <li>
    <% if (c && c._id) { %>
    <a href="/communities/<%= c._id.toString() %>">
      <strong><%= c.name %></strong>
    </a>
    — Match Score: <%= c.score %>
    <% } else { %>
    <em>Invalid community entry</em>
    <% } %>
  </li>
  <% }) %>
</ul>
<% } else { %>
<p>No matching communities found.</p>
<% } %>